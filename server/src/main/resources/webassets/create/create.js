var base = base || {};
base.createRideController = function() {
    var controller = {
        load: function () {
        }
    };
    //TODO: the controller object needs a load function
    return controller;
};