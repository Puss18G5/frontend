var base = base || {};
base.joinRideController = function() {
    var controller = {
        load: function () {
        }
    };
    //TODO: the controller object needs a load function
    return controller;
};